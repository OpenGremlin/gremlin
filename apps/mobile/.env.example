# Local web development config (not needed for native — use QR code onboarding)
# Copy to .env and fill in your values
EXPO_PUBLIC_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com
EXPO_PUBLIC_COGNITO_CLIENT_ID=your-client-id
