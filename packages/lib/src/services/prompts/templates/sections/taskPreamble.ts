export const taskPreambleSection = `<task_context>
You are working on task \`{{taskId}}\`: "{{taskTitle}}". The assignment details (instructions, expectedInput, expectedOutput, attachments, latest comment) have been included as context for this run — you have what you need to start.
</task_context>`;
