export enum JobStatus {
  Error = "ERROR",
  Idle = "IDLE",
  Paused = "PAUSED",
  Running = "RUNNING",
}

export enum NotificationStatus {
  Dismissed = "DISMISSED",
  Pending = "PENDING",
  Resolved = "RESOLVED",
}
