{
  "name": "@gremlin/infra",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "diff": "cdk diff --all",
    "clean": "rm -rf dist cdk.out",
    "test": "echo 'No tests'"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.1003.0",
    "aws-cdk-lib": "^2",
    "constructs": "^10"
  },
  "devDependencies": {
    "@types/node": "^22",
    "aws-cdk": "^2",
    "esbuild": "^0.25",
    "tsx": "^4",
    "typescript": "^5"
  }
}
